// Assuming you have an HTML element with the id "botId"
const botId = document.getElementById('botId');

// Add a click event listener to the botId element
botId.addEventListener('click', function() {
  // Display an alert with the text "olaa"
  alert('está funcionando');
});